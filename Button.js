document.createElement

export class Button {
    constructor(element) {

    }

    handleEvent(e){
        switch (e.type) {
            case 'click':
                    // dispatch event
                    // set active class to button
                break;
        
            default:
                break;
        }
    }
}